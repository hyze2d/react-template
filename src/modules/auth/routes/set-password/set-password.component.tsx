import { SetPasswordProps } from './set-password.props';
import React, { FC } from 'react';
import classNames from 'classnames';
import styles from './set-password.scss';

/**
 * <SetPassword />
 */
const SetPassword: FC<SetPasswordProps> = () => (
  <div className={styles.setPassword}>
    {/* content */}
    {/* content */}
    {/* content */}
    set password
  </div>
);

export { SetPassword };
